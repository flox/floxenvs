{
  "name": "floxenvs (pick a specific env)",
  "image": "ghcr.io/flox/flox:v1.12.0",
  "runArgs": ["--security-opt", "seccomp=unconfined"],
  "remoteUser": "root",
  "postCreateCommand": "echo 'This is the floxenvs repo root. To open a specific environment, create a Codespace via .devcontainer/<env>/devcontainer.json instead. See README.md.'"
}
