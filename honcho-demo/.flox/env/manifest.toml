schema-version = "1.17.0"

description = '''
# Honcho
Self-hosted memory infrastructure for stateful agents

```text
flox services start             — postgres + redis + api + deriver
python quickstart.py            — run the Store → Reason → Query loop
```

```text
Reasoning endpoints need an LLM key:
  export LLM_OPENAI_API_KEY=sk-...
```
'''

[include]
environments = [{ dir = "../honcho" }]

[hook]
on-activate = '''
if [[ "${FLOX_ENVS_TESTING:-}" != "1" ]]; then
  echo "  curl $HONCHO_URL/health         — health probe (no LLM key needed)"
fi
'''

[options]
systems = ["aarch64-darwin", "aarch64-linux", "x86_64-linux"]
