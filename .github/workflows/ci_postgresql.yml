name: "CI: postgresql"

on:
  push:
    branches: ["main"]
    paths:
      - "postgresql/**"
      - "postgresql-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
      - ".github/workflows/ci_postgresql.yml"
  pull_request:
    paths:
      - "postgresql/**"
      - "postgresql-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
      - ".github/workflows/ci_postgresql.yml"
  workflow_dispatch:

permissions:
  contents: "read"
  packages: "write"
  attestations: "write"
  id-token: "write"

jobs:
  run:
    uses: "./.github/workflows/environment.yml"
    with:
      environment: "postgresql"
    secrets: inherit
