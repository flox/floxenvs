name: "CI: temporal"

on:
  push:
    branches: ["main"]
    paths:
      - "temporal/**"
      - "temporal-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
      - ".github/workflows/ci_temporal.yml"
  pull_request:
    paths:
      - "temporal/**"
      - "temporal-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
      - ".github/workflows/ci_temporal.yml"
  workflow_dispatch:

permissions:
  contents: "read"
  packages: "write"
  attestations: "write"
  id-token: "write"

jobs:
  run:
    uses: "./.github/workflows/environment.yml"
    with:
      environment: "temporal"
    secrets: inherit
