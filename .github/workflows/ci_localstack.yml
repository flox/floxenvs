name: "CI: localstack"

on:
  push:
    branches: ["main"]
    paths:
      - "localstack/**"
      - "localstack-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
  pull_request:
    paths:
      - "localstack/**"
      - "localstack-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
  workflow_dispatch:

permissions:
  contents: "read"
  packages: "write"
  attestations: "write"
  id-token: "write"

jobs:
  run:
    uses: "./.github/workflows/environment.yml"
    with:
      environment: "localstack"
    secrets: inherit
