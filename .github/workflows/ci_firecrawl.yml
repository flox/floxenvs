name: "CI: firecrawl"

on:
  push:
    branches: ["main"]
    paths:
      - "firecrawl/**"
      - "firecrawl-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
      - ".github/workflows/ci_firecrawl.yml"
  pull_request:
    paths:
      - "firecrawl/**"
      - "firecrawl-demo/**"
      - "flake.nix"
      - "flake.lock"
      - "scripts/**"
      - ".github/workflows/environment.yml"
      - ".github/workflows/ci_firecrawl.yml"
  workflow_dispatch:

permissions:
  contents: "read"
  packages: "write"
  attestations: "write"
  id-token: "write"

jobs:
  run:
    uses: "./.github/workflows/environment.yml"
    with:
      environment: "firecrawl"
    secrets: inherit
