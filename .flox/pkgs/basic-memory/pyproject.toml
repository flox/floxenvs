[project]
name = "basic-memory-flox"
version = "0.0.0"
description = "Flox wrapper to lock basic-memory + transitive deps"
requires-python = ">=3.12"
dependencies = ["basic-memory"]

[tool.uv.sources]
basic-memory = { git = "https://github.com/basicmachines-co/basic-memory.git", tag = "v0.22.0" }
