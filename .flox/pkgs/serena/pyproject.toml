[project]
name = "serena-flox"
version = "0.0.0"
description = "Flox wrapper to lock serena-agent"
requires-python = ">=3.11, <3.14"
dependencies = ["serena-agent"]

[tool.uv.sources]
serena-agent = { git = "https://github.com/oraios/serena.git", tag = "v1.5.3" }
